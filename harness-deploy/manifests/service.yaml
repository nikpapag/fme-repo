apiVersion: v1
kind: Service
metadata:
  name: {{lower .Values.name}}-{{lower .Values.project_domain}}-service
spec:
  type: {{ .Values.serviceType }}
  ports:
    - name: {{lower .Values.name}}-{{lower .Values.project_domain}}-api
      protocol: TCP
      port: {{ .Values.servicePort }}
      targetPort: {{ .Values.serviceTargetPort }} 
  selector:
    app: {{lower .Values.name}}-{{lower .Values.project_domain}}
