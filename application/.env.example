# Copy to .env and set your Harness FME server-side SDK key (staging).
# Harness → FME → SDK API Keys → server-side key for staging
SPLIT_SDK_KEY=g8rntii8ovr3ink0hqi5lv2e0j1mp5guvm4l

# Optional overrides
# SPLIT_TRAFFIC_TYPE=user
# SPLIT_EVENTS_ENABLED=true
# SPLIT_EVENT_FLUSH_INTERVAL_MS=10000
# SERVER_PORT=8080
